[build]
  # Ensure a clean install then build so Nitro's prebuild runs reliably on Netlify
  command = "npm ci && npm run build"
  publish = ".output/public"

[functions]
  # No serverless functions required; this is a static prebuilt deploy.
